// Write a program which takes a character as input and print its ASCII code.
#include<stdio.h>
int main(){

    char C;
    scanf("%c",&C);                 // i do this with use of input/output func so user can input 
    printf("ASCII : %d",C);
    return 0;
}