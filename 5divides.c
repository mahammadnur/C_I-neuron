# include <stdio.h>
int main (){
    int number;
    scanf("%d", &number);
        if(number % 5){


        
        printf("%d is not divisible by 5",number);
        }
        else{
            printf("%d divisible by 5 ", number);
        }
    return 0;
}